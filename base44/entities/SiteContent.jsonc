{
  "name": "SiteContent",
  "type": "object",
  "properties": {
    "clientSlug": {
      "type": "string",
      "description": "Unique site identifier, e.g. holdsworth"
    },
    "document": {
      "type": "object",
      "description": "Full CMS site document JSON"
    },
    "draft": {
      "type": "object",
      "description": "Latest autosaved draft (unsaved editor work) \u2014 survives refresh even if localStorage is cleared"
    },
    "updatedAt": {
      "type": "string",
      "description": "ISO timestamp of last publish"
    }
  },
  "required": [
    "clientSlug",
    "document"
  ]
}